<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rest.test.mapper.UserMapper">

	<insert id="insertUser" useGeneratedKeys="true" keyColumn="UI_NUM" keyProperty="uiNum">
		INSERT INTO USER_INFO
		(UI_ID, UI_PWD, UI_NAME)
		VALUES(#{uiId}, #{uiPwd}, #{uiName})
	</insert>
	<update id="updateUser">
		UPDATE USER_INFO
		SET UI_ID=#{uiId},
		UI_NAME = #{uiName}
		WHERE UI_NUM=#{uiNum}
	</update>
	<select id="selectUserForLogin" resultType="com.rest.test.vo.UserVO">
		SELECT * FROM 
		USER_INFO 
		WHERE UI_ID=#{uiId}
		AND UI_PWD=#{uiPwd}
	</select>
	<select id="selectUsers" resultType="com.rest.test.vo.UserVO">
		SELECT UI_NUM, UI_ID, UI_NAME, UI_PWD, DATE_FORMAT(CREDAT,'%Y-%m-%d') CREDAT FROM 
		USER_INFO 
		<where>
			<if test='uiId != null and uiId != ""'>
				AND UI_ID=#{uiId}
			</if>
			<if test='uiName != null and uiName != ""'>
				AND UI_NAME LIKE concat('%',#{uiName},'%')
			</if>
		</where>
	</select>
	<delete id="deleteUser">
		DELETE FROM USER_INFO WHERE UI_NUM=#{uiNum}
	</delete>
</mapper>